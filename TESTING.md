# TESTING.md — Руководство для тестировщика

Telegram Mini App — калькулятор стоимости строительства дома из газобетона.
Стек: HTML + CSS + JS (без фреймворков), хостинг Vercel, бэкенд — Vercel Serverless Functions.

---

## 1. Структура проекта

```
tg-stroyka/
├── tg-app/                   ← статика (публичная папка)
│   ├── index.html            ← единственная HTML-страница, 12 экранов
│   ├── css/app.css           ← все стили
│   ├── admin/index.html      ← веб-панель собственника
│   └── js/
│       ├── prices.js         ← fallback PRICES (живые цены из /api/prices)
│       ├── calculator.js     ← calculate(), formatPrice() и др.
│       └── app.js            ← SCREEN_ORDER, навигация, Telegram SDK
│
├── api/lead.js               ← POST /api/lead (лид → Supabase + PDF + Telegram)
├── api/prices.js             ← GET /api/prices (цены из Supabase или fallback)
├── api/admin/                ← защищённые маршруты (JWT)
├── vercel.json               ← outputDirectory="tg-app", SPA rewrite
└── .env                      ← токены и секреты (не в git)
```

---

## 2. Запуск локально

### Требования
- Node.js ≥ 18
- [Vercel CLI](https://vercel.com/docs/cli): `npm install -g vercel`

### Шаги

```bash
git clone <repo-url>
cd tg-stroyka

# Заполнить токены в .env (скопировать из .env.example)
cp .env.example .env

# Установить зависимости (PDFKit, Supabase client, JWT)
npm install

# Запустить локальный сервер с функциями
vercel dev
# → http://localhost:3000
```

> **Без Vercel CLI:** открыть `tg-app/index.html` в браузере напрямую.
> Калькулятор и все экраны работают. Отправка лида (`/api/lead`) без сервера недоступна.

---

## 3. Среды тестирования

| Среда | URL | Что проверять |
|-------|-----|---------------|
| Локальная (браузер) | `http://localhost:3000` | Все экраны, навигация, расчёт |
| Vercel preview | URL деплоя | То же + отправка лида, /api/prices |
| Telegram Mini App | `https://t.me/Kalkulator_stroy_bot` | Полный флоу, нативные кнопки TG |
| Веб-панель | `/admin/` | Вход, лиды, цены |

> В браузере Telegram SDK загружается, но `initData` пустой — приложение переключается в браузерный режим (синяя кнопка `#browser-main-btn` вместо MainButton).

---

## 4. Функциональное тестирование по экранам

### Общие правила навигации
- **Вперёд:** MainButton (Telegram) или синяя кнопка внизу (браузер)
- **Назад:** BackButton в хедере (Telegram) или кнопка браузера
- Анимация перехода: 280 мс, появляется справа/слева

---

### Экран 0 — Старт (`screen-0`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 0.1 | Открыть в браузере | Иконка дома, заголовок, регион, список доверия, tagline |
| 0.2 | Открыть в Telegram | То же + «Привет, [Имя] 👋» над заголовком |
| 0.3 | Открыть первый раз | Через 600 мс — модальный оффер «🎁 Скидка 15%» |
| 0.4 | Нажать «Пропустить» | Оффер исчезает (opacity→0 за 220 мс) |
| 0.5 | Нажать «Получить скидку 15%» | Переход в Telegram по ссылке, оффер закрывается |
| 0.6 | Тап по фону оффера | Оффер закрывается |
| 0.7 | Перезагрузить (оффер уже был) | Оффер не появляется — `localStorage.offer_shown = '1'` |
| 0.8 | Нажать «✦ Начать расчёт» | Переход на `screen-region` |

Сброс оффера: `localStorage.removeItem('offer_shown')` в консоли.

---

### Экран Регион (`screen-region`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| R.1 | Открыть | 4 карточки: Калужская обл., Обнинск, Московская обл., Новая Москва |
| R.2 | Карточки Московская / Новая Москва | Бейдж «+15%» |
| R.3 | Нажать «Далее →» без выбора | Кнопка мигает, переход не происходит |
| R.4 | Выбрать любой регион | Карточка выделяется рамкой |
| R.5 | Нажать «Далее →» | Переход на `screen-1` |

---

### Экран 1 — Площадь + Этажность (`screen-1`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1.1 | Открыть | Слайдер на 120, чип «120» активен, прогресс 28.6% |
| 1.2 | «Далее →» без этажности | Кнопка мигает |
| 1.3 | Двигать слайдер | Значение обновляется, синяя заливка меняется |
| 1.4 | Нажать чип «80» | Слайдер → 80, чип «80» активен |
| 1.5 | Выбрать «1 этаж» | Карточка выделяется, кнопка становится активной |
| 1.6 | «Далее →» | Переход на `screen-2` |

---

### Экран 2 — Фундамент (`screen-2`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 2.1 | Открыть | Прогресс 42.9%, 4 карточки 2×2 |
| 2.2 | «Далее →» без выбора | Кнопка мигает |
| 2.3 | Выбрать фундамент | Карточка выделяется |
| 2.4 | «Далее →» | Переход на `screen-3` |

---

### Экран 3 — Кровля (`screen-3`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 3.1 | Открыть | Прогресс 57.1%, блок превью скрыт |
| 3.2 | «Далее →» без выбора | Кнопка мигает |
| 3.3 | Выбрать только материал | Кнопка мигает — нужны оба поля |
| 3.4 | Выбрать материал + форму | Блок «Ориентировочно: X млн ₽» появляется |
| 3.5 | Проверить превью | `box + foundation + roof` без стиля/фасада/отделки |
| 3.6 | «Далее →» | Переход на `screen-4` |

---

### Экран 4 — Стиль (`screen-4`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 4.1 | Открыть | Прогресс 71.4%, 3 вертикальные карточки |
| 4.2 | Карточки «Хай-тек» / «Шале» | Бейджи «+10%» и «+18%» |
| 4.3 | «Далее →» без выбора | Кнопка мигает |
| 4.4 | Выбрать стиль → «Далее →» | Переход на `screen-facade` |

---

### Экран Фасад (`screen-facade`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| F.1 | Открыть | Прогресс 85.7%, 4 карточки с ценами |
| F.2 | Карточки | Штукатурка 3 500/м², Кирпич 8 000/м², Панель 5 500/м², Без фасада 0 |
| F.3 | «Далее →» без выбора | Кнопка мигает |
| F.4 | Выбрать фасад → «Далее →» | Переход на `screen-5` |

---

### Экран 5 — Отделка (`screen-5`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 5.1 | Открыть | Прогресс 100%, 5 горизонтальных карточек |
| 5.2 | Проверить карточки | Коробка (0), Черновая (7 000/м²), Вайт бокс (10 000/м²), Под ключ Эконом (12 000/м²), Под ключ Стандарт (22 000/м²) |
| 5.3 | «Далее →» без выбора | Кнопка мигает |
| 5.4 | Выбрать отделку → «Далее →» | Переход на `screen-6` |

---

### Экран 6 — Опции (`screen-6`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 6.1 | Открыть | «Последний шаг», 3 опции, бегущий итог |
| 6.2 | «⚡ Рассчитать» без опций | Переход на `screen-7` (опции не обязательны) |
| 6.3 | Выбрать «Терраса» | Чекбокс отмечен, итог +200 000 ₽ |
| 6.4 | Снять «Терраса» | Итог уменьшается |
| 6.5 | Все 3 опции | Итог = базовый + 200 000 + 380 000 + 420 000 |

---

### Экран 7 — Результат (`screen-7`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 7.1 | Открыть | Шапка «🏠 X м² · этажность», строки разбивки, итог, ипотека |
| 7.2 | Проверить разбивку | Строки: Фундамент, Коробка, Кровля; при стиле ≠ классика — «Надбавка за стиль»; при выбранном фасаде — строка Фасада; при отделке ≠ коробка — строка Отделки; при московском регионе — «Региональная надбавка +15%»; при опциях — их строки |
| 7.3 | Проверить итог вручную | См. раздел 5 «Проверка расчёта» |
| 7.4 | «🔄 Пересчитать» | Сброс всех выборов (включая регион и фасад), возврат на `screen-region` |
| 7.5 | «👥 Поделиться» в браузере | `navigator.share()` или ничего если не поддерживается |
| 7.6 | «👥 Поделиться» в Telegram | Диалог пересылки с текстом + ссылкой |
| 7.7 | «Узнать точную стоимость» | Переход на `screen-8` |

---

### Экран 8 — Форма контакта (`screen-8`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 8.1 | Открыть | Поле «Имя» заполнено именем из Telegram (если в TG) |
| 8.2 | «Отправить» при пустых полях | Кнопка мигает |
| 8.3 | Имя 1 символ | Кнопка неактивна (минимум 2) |
| 8.4 | Имя ≥ 2, телефон пустой | Кнопка неактивна |
| 8.5 | Ввести «9161234567» | Форматируется в «+7 (916) 123-45-67» |
| 8.6 | Ввести «8161234567» | «8» заменяется на «7» |
| 8.7 | Телефон < 11 цифр | Кнопка неактивна |
| 8.8 | Без галочки согласия | Кнопка неактивна |
| 8.9 | Всё корректно | «Отправить заявку» активна |
| 8.10 | Нажать «Отправить» | POST `/api/lead`, переход на `screen-9` |
| 8.11 | TELEGRAM_CHAT_ID не задан | Ответ 500 + сообщение «Ошибка отправки...» |

---

### Экран 9 — Спасибо (`screen-9`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 9.1 | Открыть | «✅ Заявка отправлена!», текст с именем менеджера |
| 9.2 | Supabase настроен | Кнопка «📄 Скачать смету PDF» видима, ведёт на Supabase Storage URL |
| 9.3 | Supabase не настроен | Кнопка PDF скрыта (`pdfUrl: null`) |
| 9.4 | «Смотреть проекты» в Telegram | `tgApp.openTelegramLink('https://t.me/yourchannel')` — заглушка |

---

## 5. Проверка расчёта вручную

Актуальная формула (из [tg-app/js/calculator.js](tg-app/js/calculator.js)):

```
box         = area × PRICES.boxPerSqm[floors]
foundation  = area × PRICES.foundation[foundation]
roof        = box × roofMaterial_коэф × roofShape_коэф − box
structural  = box + foundation + roof

styleSurch  = structural × (PRICES.style[style] − 1)
facadeCost  = area × PRICES.facade[facade]         ← НОВОЕ
finishing   = area × PRICES.finishing[finishing]
options     = сумма PRICES.options[o]

regionBase  = structural + styleSurch + facadeCost + finishing
regionSurch = regionBase × (PRICES.region[region] − 1)   ← НОВОЕ

total       = regionBase + regionSurch + options    ← опции вне регион. коэф.
perSqm      = round(total / area)
mortgage    = round(total × 0.005 / (1 − 1.005^−240))
```

### Эталонный пример — минимальный

**Параметры:** 120 м², 1 этаж, Калуга, свайный, металлочерепица, двускатная, классика, без фасада, коробка, без опций

```
box        = 120 × 22 000 = 2 640 000
foundation = 120 × 4 000  =   480 000
roof       = 2 640 000 × 1.00 × 1.00 − 2 640 000 = 0
structural = 3 120 000
styleSurch = 3 120 000 × 0 = 0
facade     = 120 × 0 = 0
finishing  = 120 × 0 = 0
regionBase = 3 120 000
regionSurch= 3 120 000 × 0 = 0  (Калуга ×1.00)
total      = 3 120 000 ₽
perSqm     = 26 000 ₽/м²
mortgage   ≈ 22 376 ₽/мес
```

### Пример с регионом и фасадом

**Параметры:** 100 м², 1 этаж, Московская обл., свайный, металлочерепица, двускатная, классика, штукатурка (3 500/м²), черновая (7 000/м²), без опций

```
box        = 100 × 22 000 = 2 200 000
foundation = 100 × 4 000  =   400 000
roof       = 0
structural = 2 600 000
styleSurch = 0
facade     = 100 × 3 500 = 350 000
finishing  = 100 × 7 000 = 700 000
regionBase = 2 600 000 + 0 + 350 000 + 700 000 = 3 650 000
regionSurch= 3 650 000 × 0.15 = 547 500
total      = 3 650 000 + 547 500 + 0 = 4 197 500 ₽
perSqm     = 41 975 ₽/м²
```

### Полный пример (с надбавками)

**Параметры:** 150 м², мансарда, Обнинск, УШП, мягкая черепица, 4-скатная, шале, кирпич (8 000/м²), под ключ стандарт (22 000/м²), терраса + баня

```
box        = 150 × 20 000 = 3 000 000
foundation = 150 × 16 000 = 2 400 000
roof       = 3 000 000 × 1.08 × 1.12 − 3 000 000 = 621 600
structural = 6 021 600
styleSurch = 6 021 600 × 0.18 = 1 083 888
facade     = 150 × 8 000  = 1 200 000
finishing  = 150 × 22 000 = 3 300 000
regionBase = 6 021 600 + 1 083 888 + 1 200 000 + 3 300 000 = 11 605 488
regionSurch= 11 605 488 × 0 = 0  (Обнинск ×1.00)
options    = 200 000 + 420 000 = 620 000
total      = 11 605 488 + 620 000 = 12 225 488 ₽
```

---

## 6. Тестирование серверной функции `/api/lead`

### curl-тест (требует `vercel dev`)

```bash
curl -X POST http://localhost:3000/api/lead \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван",
    "phone": "+7 (916) 123-45-67",
    "region": "kaluga",
    "area": 120,
    "floors": "single",
    "foundation": "pile",
    "roofMaterial": "metalTile",
    "roofShape": "gable",
    "style": "classic",
    "facade": "none",
    "finishing": "shell",
    "options": [],
    "total": 3120000,
    "breakdown": {"foundation":480000,"box":2640000,"roof":0,"style":0,"facade":0,"finishing":0,"options":0,"region":0},
    "perSqm": 26000,
    "mortgage": 22376
  }'
```

**Ожидаемые ответы:**

| Условие | HTTP | Тело |
|---------|------|------|
| `BOT_TOKEN` или `CHAT_ID` не заданы | 500 | `{"ok":false,"error":"Server not configured"}` |
| Нет `name` или `phone` | 400 | `{"ok":false,"error":"name and phone are required"}` |
| Все OK, Supabase настроен | 200 | `{"ok":true,"pdfUrl":"https://..."}` |
| Все OK, без Supabase | 200 | `{"ok":true,"pdfUrl":null}` |
| Telegram API недоступен | 502 | `{"ok":false,"error":"Failed to send notification"}` |

### Ожидаемое сообщение в Telegram

```
🏠 Новая заявка — Калькулятор

👤 Имя: Иван
📞 Телефон: +7 (916) 123-45-67
💬 Telegram: @username (если из TG)

🗺 Регион: Калужская обл.
📐 Площадь: 120 м², 1 этаж
🧱 Материал: Газобетон
🏗 Фундамент: Свайно-винтовой
🏠 Кровля: Металлочерепица, Двускатная
🎨 Стиль: Классика
🏛 Фасад: Без фасада
🔨 Отделка: Коробка
✨ Опции: Нет

💰 Расчёт клиента: ≈ 3 120 000 ₽
🔑 ID: uuid (если Supabase настроен)

🕐 ДД.ММ.ГГГГ, ЧЧ:ММ МСК
```

---

## 7. Тестирование `/api/prices`

```bash
curl http://localhost:3000/api/prices
```

**Ожидаемый ответ:**
```json
{
  "ok": true,
  "source": "fallback",
  "prices": {
    "boxPerSqm": { "single": 22000, ... },
    "facade": { "plaster": 3500, "brick": 8000, "panel": 5500, "none": 0 },
    "region": { "kaluga": 1.00, "moscow_obl": 1.15, ... },
    ...
  }
}
```

Если Supabase настроен — `"source": "db"`.

---

## 8. Тестирование веб-панели `/admin/`

### Вход

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| A.1 | Открыть `/admin/` | Форма входа с полем пароля |
| A.2 | Неверный пароль | «Неверный пароль» |
| A.3 | Верный пароль | Вход, появляется сайдбар с «Заявки» и «Цены» |
| A.4 | Обновить страницу после входа | Остаётся в панели (JWT в localStorage) |
| A.5 | Нажать «Выйти» | Очищается JWT, показывается форма входа |

### Раздел Заявки

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| A.6 | Открыть «Заявки» | Таблица лидов, счётчики (всего/новые/позвонили/приняли) |
| A.7 | Поиск по имени/телефону | Таблица фильтруется |
| A.8 | Нажать строку лида | Открывается модалка с деталями |
| A.9 | Сменить статус | PATCH `/api/admin/leads/:id`, бейдж обновляется |
| A.10 | Сохранить заметки | PATCH с `notes`, OK без перезагрузки |

### Раздел Цены

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| A.11 | Открыть «Цены» | Форма с текущими значениями (из Supabase) |
| A.12 | Изменить цену, нажать «Сохранить» | PUT `/api/admin/prices`, кнопка на секунду меняет текст |
| A.13 | Проверить в Mini App | `GET /api/prices` возвращает обновлённые цены |

---

## 9. Тестирование в Telegram

### Специфичные для Telegram проверки

| # | Что проверять | Ожидаемый результат |
|---|---------------|---------------------|
| T.1 | MainButton | Присутствует на всех экранах, текст меняется по шагам |
| T.2 | BackButton | Видима на шагах 1–10 (screen-region ... screen-8) |
| T.3 | Приветствие | «Привет, [Имя] 👋» на экране 0 |
| T.4 | Предзаполнение | Поле «Имя» на screen-8 заполнено из профиля TG |
| T.5 | Тактильная обратная связь | Вибрация при выборе карточек |
| T.6 | Вибрация при ошибке | При кнопке без выбора — тип «error» |
| T.7 | Подтверждение закрытия | После screen-2 при закрытии — диалог |
| T.8 | Тёмная тема | Класс `dark-theme` на body |
| T.9 | Кнопка «Поделиться» | Открывает диалог TG с текстом и ссылкой |

---

## 10. Регрессионные проверки

После любых изменений:

- [ ] Оффер не показывается повторно (`localStorage.offer_shown`)
- [ ] Слайдер и chips синхронны
- [ ] Кнопка «Пересчитать» сбрасывает **все** выборы включая регион и фасад, возврат на `screen-region`
- [ ] Форма не отправляется с невалидными данными
- [ ] Региональный коэффициент применяется только к `structural + style + facade + finishing`, **не** к опциям
- [ ] При `facade: 'none'` строка фасада не показывается в результате
- [ ] `GET /api/prices` возвращает ключи `facade` и `region` в объекте prices
- [ ] `/admin/` открывается по URL без перезагрузки в `index.html` калькулятора

---

## 11. Переменные окружения

| Переменная | Где задать | Что содержит |
|------------|------------|--------------|
| `TELEGRAM_BOT_TOKEN` | Vercel Dashboard / `.env` | Токен бота из BotFather |
| `TELEGRAM_CHAT_ID` | Vercel Dashboard / `.env` | chat_id собственника (→ @userinfobot) |
| `TELEGRAM_OWNER_ID` | Vercel Dashboard / `.env` | user_id собственника (для /addphoto) |
| `TELEGRAM_WEBHOOK_SECRET` | Vercel Dashboard / `.env` | Секрет валидации webhook |
| `SUPABASE_URL` | Vercel Dashboard / `.env` | URL Supabase проекта |
| `SUPABASE_SERVICE_KEY` | Vercel Dashboard / `.env` | service_role ключ |
| `ADMIN_PASSWORD` | Vercel Dashboard / `.env` | Пароль для `/admin/` |
| `JWT_SECRET` | Vercel Dashboard / `.env` | Секрет подписи JWT (≥32 символа) |

---

## 12. Известные ограничения

| # | Ситуация | Поведение |
|---|----------|-----------|
| K.1 | Кнопка «Смотреть проекты» ведёт на `t.me/yourchannel` | Заглушка — нужно заменить реальным каналом в `app.js` |
| K.2 | Supabase не настроен | PDF-кнопка скрыта, лиды только в Telegram |
| K.3 | TELEGRAM_CHAT_ID не задан | Форма возвращает 500 |
| K.4 | «Поделиться» в Chrome Desktop | `navigator.share()` не работает — ожидаемо (только мобильные браузеры) |
| K.5 | Экран портфолио не реализован | `GET /api/portfolio` готов, экран в Mini App — нет |

---

## 13. Шаблон баг-репорта

```
Экран: [ID и название, например screen-facade — Фасад]
Среда: [Chrome 124 / iPhone 14 / Telegram iOS 10.x]
Шаги воспроизведения:
  1. ...
  2. ...
Ожидаемый результат: ...
Фактический результат: ...
Скриншот / лог консоли: [приложить]
```
