# TESTING.md — Руководство для тестировщика

Telegram Mini App — калькулятор стоимости строительства дома из газобетона.  
Стек: HTML + CSS + JS (без фреймворков), хостинг Netlify, бэкенд — Netlify Functions.

---

## 1. Структура проекта

```
tg-stroyka/
├── tg-app/                   ← статика (публичная папка)
│   ├── index.html            ← единственная HTML-страница, 10 экранов
│   ├── css/app.css           ← все стили
│   └── js/
│       ├── prices.js         ← все цены (константа PRICES)
│       ├── calculator.js     ← функции calculate(), formatPrice() и др.
│       └── app.js            ← навигация, Telegram SDK, обработчики
│
├── netlify/functions/lead.js ← serverless-функция приёма лида
├── netlify.toml              ← publish = "tg-app", functions = "netlify/functions"
└── .env                      ← TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID (не в git)
```

---

## 2. Запуск локально

### Требования
- Node.js ≥ 18 (нужен только для Netlify CLI)
- [Netlify CLI](https://docs.netlify.com/cli/get-started/): `npm install -g netlify-cli`

### Шаги

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd tg-stroyka

# 2. Создать .env (если нет) — скопировать из шаблона
cp .env.example .env
# Заполнить TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID в .env

# 3. Запустить локальный сервер с поддержкой функций
netlify dev
# → откроется http://localhost:8888
```

> **Без Netlify CLI:** открыть `tg-app/index.html` напрямую в браузере.  
> Приложение работает в браузере — вместо Telegram MainButton внизу появится синяя кнопка-заглушка.  
> Отправка лида (`/.netlify/functions/lead`) в этом режиме не работает без сервера.

---

## 3. Среды тестирования

| Среда | URL | Что проверять |
|-------|-----|---------------|
| Локальная (браузер) | `http://localhost:8888` | Все экраны, навигация, калькулятор |
| Netlify/Vercel preview | URL деплоя | Те же + отправка лида |
| Telegram Mini App | `https://t.me/Kalkulator_stroy_bot` → кнопка «Открыть» | Полный флоу, нативные кнопки TG |

> В браузере Telegram SDK загружается, но `initData` будет пустой строкой — приложение корректно переключается в браузерный режим (строка 8–9 `app.js`).

---

## 4. Функциональное тестирование по экранам

### Общие правила навигации
- **Вперёд:** кнопка внизу (в браузере — синяя кнопка `browser-main-btn`, в Telegram — `MainButton`)
- **Назад:** кнопка «Назад» (в браузере — стандартная Back, в Telegram — `BackButton` в хедере)
- Анимация перехода: 280 мс, новый экран появляется справа/слева

---

### Экран 0 — Старт (`screen-0`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 0.1 | Открыть приложение в браузере | Отображается: иконка дома, заголовок «СтройДом», регион, список из 3 пунктов, tagline |
| 0.2 | Открыть в Telegram | То же + над заголовком появляется «Привет, [Имя] 👋» (имя берётся из профиля TG) |
| 0.3 | Открыть **первый раз** (localStorage пуст) | Через 600 мс появляется модальное окно оффера (тёмный оверлей + карточка с «🎁 Скидка 15%...») |
| 0.4 | Нажать «Пропустить» в оффере | Модальное окно исчезает с анимацией (opacity → 0 за 220 мс) |
| 0.5 | Нажать «Получить скидку 15%» | Переход по ссылке `https://t.me/kalkulator_stroy_bot?start=from_app`; оффер закрывается |
| 0.6 | Тап по фону оффера (вне карточки) | Оффер закрывается |
| 0.7 | Перезагрузить страницу (оффер уже был закрыт) | Оффер **не** появляется — значение `offer_shown` в `localStorage` равно `'1'` |
| 0.8 | Нажать кнопку «✦ Начать расчёт» | Переход на `screen-1` |

**Сброс оффера для повторного теста:** `localStorage.removeItem('offer_shown')` в консоли браузера.

---

### Экран 1 — Площадь и этажность (`screen-1`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1.1 | Открыть экран | Слайдер установлен на 120, чип «120» активен, прогресс-бар 16.7% |
| 1.2 | Нажать «Далее →» без выбора этажности | Кнопка мигает (opacity → 0.5 → 1), переход **не** происходит |
| 1.3 | Двигать слайдер | Отображаемое значение обновляется, синяя заливка слайдера меняется пропорционально |
| 1.4 | Нажать чип «80» | Слайдер переключается на 80, чип «80» становится активным, «120» снимается |
| 1.5 | Нажать карточку «1 этаж» | Карточка выделяется синей рамкой, кнопка «Далее» становится активной |
| 1.6 | Нажать другую этажность | Предыдущая снимается, новая выделяется |
| 1.7 | Нажать «Далее →» после выбора этажности | Переход на `screen-2` |

---

### Экран 2 — Фундамент (`screen-2`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 2.1 | Открыть экран | Прогресс-бар 33.3%, 4 карточки в сетке 2×2 |
| 2.2 | Нажать «Далее →» без выбора | Кнопка мигает, переход не происходит |
| 2.3 | Нажать любую карточку фундамента | Карточка выделяется |
| 2.4 | Нажать «Далее →» | Переход на `screen-3` |

---

### Экран 3 — Кровля (`screen-3`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 3.1 | Открыть экран | Прогресс-бар 50%, два ряда карточек (Материал / Форма), блок превью скрыт |
| 3.2 | Нажать «Далее →» без выбора | Кнопка мигает |
| 3.3 | Нажать «Далее →» выбрав только материал | Кнопка мигает — нужен выбор обоих полей |
| 3.4 | Выбрать материал и форму | Появляется блок «Ориентировочно: X млн ₽» |
| 3.5 | Проверить превью | Значение должно отражать: площадь × цена_коробки + площадь × цена_фундамента + стоимость_кровли |
| 3.6 | Нажать «Далее →» | Переход на `screen-4` |

---

### Экран 4 — Стиль (`screen-4`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 4.1 | Открыть экран | Прогресс-бар 66.7%, 3 вертикальные карточки с SVG-силуэтами |
| 4.2 | Карточки «Хай-тек» и «Шале» | Показывают бейдж «+10%» и «+18%» соответственно |
| 4.3 | Нажать «Далее →» без выбора | Кнопка мигает |
| 4.4 | Выбрать стиль → «Далее →» | Переход на `screen-5` |

---

### Экран 5 — Отделка (`screen-5`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 5.1 | Открыть экран | Прогресс-бар 83.3%, 4 горизонтальные карточки |
| 5.2 | Нажать «Далее →» без выбора | Кнопка мигает |
| 5.3 | Выбрать отделку → «Далее →» | Переход на `screen-6` |

---

### Экран 6 — Опции (`screen-6`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 6.1 | Открыть экран | Прогресс-бар 100%, 3 опции с ценами, бегущий итог внизу |
| 6.2 | Нажать кнопку «⚡ Рассчитать» без выбора опций | Переход на `screen-7` (опции не обязательны) |
| 6.3 | Выбрать «Терраса» | Чекбокс становится отмеченным, итог обновляется (+200 000 ₽) |
| 6.4 | Повторно нажать «Терраса» | Снимается выбор, итог уменьшается |
| 6.5 | Выбрать все 3 опции, проверить итог | Итог = базовый расчёт + 200 000 + 380 000 + 420 000 ₽ |

---

### Экран 7 — Результат (`screen-7`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 7.1 | Открыть экран | Шапка «🏠 [area] м² · [этажность]», строки разбивки, итог, ипотека |
| 7.2 | Проверить разбивку | Строки: Фундамент, Коробка, Кровля; при стиле ≠ классика — «Надбавка за стиль»; при отделке ≠ коробка — строка отделки; при выбранных опциях — их перечень |
| 7.3 | Проверить итог вручную | См. раздел 5 «Проверка расчёта» |
| 7.4 | Нажать «🔄 Пересчитать» | Сброс всех выборов, возврат на `screen-1`, слайдер = 120 |
| 7.5 | Нажать «👥 Поделиться с другом» в браузере | Вызывается `navigator.share()` или ничего (если API не поддерживается) |
| 7.6 | Нажать «👥 Поделиться с другом» в Telegram | Открывается диалог отправки TG с текстом + ссылкой `https://t.me/Kalkulator_stroy_bot/app` |
| 7.7 | Нажать «Узнать точную стоимость» | Переход на `screen-8` |

---

### Экран 8 — Форма контакта (`screen-8`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 8.1 | Открыть экран | Поле «Имя» заполнено именем из Telegram (если открыто в TG) |
| 8.2 | Нажать «Отправить заявку» при пустых полях | Кнопка мигает, переход не происходит |
| 8.3 | Ввести имя 1 символ | Кнопка остаётся неактивной (минимум 2 символа) |
| 8.4 | Ввести имя ≥ 2 символов, не заполнять телефон | Кнопка неактивна |
| 8.5 | Ввести телефон: «9161234567» | Автоматически форматируется в «+7 (916) 123-45-67» |
| 8.6 | Ввести телефон начиная с «8» | «8161234567» → «+7 (716) 123-45-67» (8 заменяется на 7) |
| 8.7 | Ввести телефон < 11 цифр | Кнопка неактивна |
| 8.8 | Не поставить галочку согласия | Кнопка неактивна |
| 8.9 | Заполнить всё корректно | Кнопка «Отправить заявку» становится активной |
| 8.10 | Нажать «Отправить заявку» | Отправляется POST на `/.netlify/functions/lead`, переход на `screen-9` |
| 8.11 | Отправка при `TELEGRAM_CHAT_ID` не задан | Ответ 500 + сообщение «Ошибка отправки. Попробуйте ещё раз.» под формой (скрывается через 5 с) |

---

### Экран 9 — Спасибо (`screen-9`)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 9.1 | Открыть экран | «✅ Заявка отправлена!», текст с именем менеджера |
| 9.2 | Нажать «Смотреть проекты» (в Telegram) | `tgApp.openTelegramLink('https://t.me/yourchannel')` — **сейчас это заглушка, реальный канал не подставлен** |

---

## 5. Проверка расчёта вручную

Формула (из `calculator.js`):

```
box        = area × PRICES.boxPerSqm[floors]
foundation = area × PRICES.foundation[foundation]
roof       = box × roofMaterial_коэф × roofShape_коэф − box
structural = box + foundation + roof
style      = structural × (PRICES.style[style] − 1)
finishing  = area × PRICES.finishing[finishing]
options    = сумма PRICES.options[o] для выбранных o
total      = structural + style + finishing + options
perSqm     = total / area
mortgage   = total × (0.005) / (1 − (1.005)^−240)
```

### Эталонный пример (для проверки)

Параметры: **120 м², 1 этаж, свайный фундамент, металлочерепица, двускатная, классика, коробка, без опций**

```
box        = 120 × 22 000 = 2 640 000
foundation = 120 × 4 000  =   480 000
roof       = 2 640 000 × 1.00 × 1.00 − 2 640 000 = 0
structural = 2 640 000 + 480 000 + 0 = 3 120 000
style      = 3 120 000 × (1.00 − 1) = 0
finishing  = 120 × 0 = 0
total      = 3 120 000 ₽
perSqm     = 26 000 ₽/м²
mortgage   ≈ 22 376 ₽/мес
```

Что проверить в UI на экране 7:
- ИТОГО: `≈ 3 120 000 ₽`
- /м²: `26 000 ₽/м²`
- Ипотека: `от ≈ 22 376 ₽/мес` (допустимое отклонение ±1 ₽ — округление)

### Второй пример (с надбавками)

Параметры: **150 м², мансарда, УШП, мягкая черепица, 4-скатная, шале, под ключ стандарт, терраса+баня**

```
box        = 150 × 20 000 = 3 000 000
foundation = 150 × 16 000 = 2 400 000
roof       = 3 000 000 × 1.08 × 1.12 − 3 000 000 = 621 600
structural = 3 000 000 + 2 400 000 + 621 600 = 6 021 600
style      = 6 021 600 × 0.18 = 1 083 888
finishing  = 150 × 22 000 = 3 300 000
options    = 200 000 + 420 000 = 620 000
total      = 6 021 600 + 1 083 888 + 3 300 000 + 620 000 = 11 025 488 ₽
```

---

## 6. Тестирование серверной функции (лид)

Функция: `netlify/functions/lead.js`  
URL: `POST /.netlify/functions/lead`

### Тест через curl (требует запущенного `netlify dev`)

```bash
curl -X POST http://localhost:8888/.netlify/functions/lead \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван",
    "phone": "+7 (916) 123-45-67",
    "area": 120,
    "floors": "single",
    "foundation": "pile",
    "roofMaterial": "metalTile",
    "roofShape": "gable",
    "style": "classic",
    "finishing": "shell",
    "options": [],
    "total": 3120000
  }'
```

**Ожидаемые ответы:**

| Условие | HTTP | Тело |
|---------|------|------|
| `TELEGRAM_BOT_TOKEN` или `TELEGRAM_CHAT_ID` не заданы | 500 | `{"ok":false,"error":"Server not configured"}` |
| Отсутствует `name` или `phone` | 400 | `{"ok":false,"error":"name and phone are required"}` |
| Невалидный JSON | 400 | `{"ok":false,"error":"Invalid JSON"}` |
| Telegram API недоступен | 502 | `{"ok":false,"error":"Failed to send notification"}` |
| Всё корректно, env-переменные заданы | 200 | `{"ok":true}` |

### Проверка сообщения в Telegram

После успешной отправки в чат менеджера должно прийти сообщение вида:

```
🏠 Новая заявка — Калькулятор

👤 Имя: Иван
📞 Телефон: +7 (916) 123-45-67
👤 Telegram: @username (если открыто из TG)

📐 Площадь: 120 м², 1 этаж
🧱 Материал: Газобетон
🏗 Фундамент: Свайно-винтовой
🏠 Кровля: Металлочерепица, Двускатная
🎨 Стиль: Классика
🔨 Отделка: Коробка
✨ Опции: Нет

💰 Расчёт клиента: ≈ 3 120 000 ₽

🕐 26.05.2026, 14:30 МСК
```

---

## 7. Тестирование в Telegram

### Подготовка
1. Открыть Telegram → найти `@Kalkulator_stroy_bot`
2. Нажать кнопку «Открыть» / «Запустить»

### Специфичные для Telegram проверки

| # | Что проверять | Ожидаемый результат |
|---|---------------|---------------------|
| T.1 | `MainButton` (синяя кнопка внизу) | Появляется на всех экранах, меняет текст при смене шага |
| T.2 | `BackButton` (стрелка в хедере) | Появляется на шагах 1–8, скрыта на 0 и 9 |
| T.3 | Приветствие по имени | На экране 0 отображается «Привет, [Имя] 👋» |
| T.4 | Предзаполнение формы | На экране 8 поле «Имя» заполнено именем из профиля TG |
| T.5 | Тактильная обратная связь | При выборе карточек ощущается вибрация (на устройствах с Haptic) |
| T.6 | Вибрация при ошибке | При попытке нажать кнопку с невыбранными параметрами — вибрация типа «error» |
| T.7 | Подтверждение закрытия | После шага 2 при попытке закрыть Mini App — диалог подтверждения |
| T.8 | Тема | При тёмной теме Telegram тело получает класс `dark-theme`, цвета адаптируются |
| T.9 | Кнопка «Поделиться» | Открывает диалог пересылки сообщения другу с текстом и ссылкой |

---

## 8. Регрессионные проверки

После любых изменений обязательно проверить:

- [ ] Оффер не показывается повторно (localStorage `offer_shown`)
- [ ] Слайдер и чипы работают синхронно (изменение в одном → обновляется другое)
- [ ] Кнопка «Пересчитать» сбрасывает ВСЕ выборы (area=120, floors/foundation/... = null, options=[])
- [ ] Форма не отправляется с невалидными данными (имя < 2 символов, телефон < 11 цифр, галочка не стоит)
- [ ] Функция `/lead` возвращает 500 при незаполненных env-переменных (не 200!)

---

## 9. Известные проблемы и ограничения

| # | Проблема | Статус |
|---|----------|--------|
| K.1 | Экран 9: кнопка «Смотреть проекты» ведёт на `https://t.me/yourchannel` — заглушка | Требует замены на реальный канал в `app.js:216` |
| K.2 | `TELEGRAM_CHAT_ID` в `.env` пустой — лиды не доходят до менеджера | Нужно получить ID через `@userinfobot` и задать в Netlify Dashboard |
| K.3 | Команда `/start` в боте не отвечает текстом | Ожидаемо: это Mini App бот, не чат-бот. Без webhook-сервера команды не обрабатываются |
| K.4 | Кнопка «Поделиться» в браузере вызывает `navigator.share()` — не работает в Chrome Desktop | Ожидаемо: Web Share API поддерживается только в мобильных браузерах и Safari |

---

## 10. Переменные окружения

| Переменная | Где задать | Что содержит |
|------------|------------|--------------|
| `TELEGRAM_BOT_TOKEN` | Netlify Dashboard → Site configuration → Environment variables | Токен бота из BotFather |
| `TELEGRAM_CHAT_ID` | Netlify Dashboard → Site configuration → Environment variables | `chat_id` менеджера — узнать через `@userinfobot` в Telegram |

Для локальной разработки — задать в файле `.env` в корне проекта (файл в `.gitignore`, в git не попадает).

---

## 11. Шаблон баг-репорта

```
Экран: [номер и название]
Браузер/устройство: [Chrome 124 / iPhone 14 / Telegram iOS]
Шаги воспроизведения:
  1. ...
  2. ...
Ожидаемый результат: ...
Фактический результат: ...
Скриншот: [приложить]
```
